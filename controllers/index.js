const contacts = require("./contacts");
const auth = require("./auth");
const users = require("./users");
const updateImg = require("./users");

module.exports = {
  contacts,
  auth,
  users,
  updateImg,
};
